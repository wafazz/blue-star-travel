# Session Memory - Blue Travel (TAMS)
> Last updated: 2026-07-19

## Session Context
- **Project**: Blue Travel — Travel & Tour Management System (Blue Star Travel And Tours)
- **Profile**: ~/Desktop/MemoryCore Project/Projects/41-blue-travel.md
- **Branch**: (no git yet)
- **Status**: active
- **Focus**: **CODE-COMPLETE** — all 10 phases (0–9) shipped. Remaining = client-side (FPX vendor, KPDN licence, deploy).

## Current Tasks
- [✔] Phase 0: Foundation — Laravel 12 + BS5 (blue) + role-based multi-portal auth, mockups→Blade.
- [✔] Phase 1: Core Catalog — company/providers/packages/customers CRUD under `/manage`.
- [✔] Phase 2: Booking Engine — creation, approval workflow, timeline, provider respond, documents.
- [✔] Phase 3: Payments & Finance — FPX sandbox gateway, payments screen, receipts, refunds, finance dashboard.
- [✔] Phase 4: Commission — dynamic-depth MLM cascade, wallet, withdrawals, reversal.
- [✔] Phase 5: Agent Gamification — missions, check-in/streak, points ledger, 7-item redeem catalog, 10 achievements (auto-unlock), monthly leaderboard, referral/network. Agent dashboard mockup now fully data-driven.
- [✔] Phase 6: Marketing & Support — notifications engine (in-app bell + channel stubs, wired to booking/commission/withdrawal/redemption/ticket/broadcast events), coupons (wired to booking discount), banners, marketing materials + agent download center, support ticketing (agent open → staff queue + threaded reply + status).
- [✔] Phase 7: Reports & Analytics — 7 reports (sales/booking/package/customer/agent/commission/financial) via ReportService, unified filter+KPI+chart+totals view, PDF/Excel/CSV export.
- [✔] Phase 8: Customer Portal — register (+referral `?ref=`), public package catalog, self-service booking, FPX/slip payment, my-trips + documents, passport profile, customer support tickets. Demo state now fully seeded (ShowcaseSeeder + CustomerPortalSeeder).
- [✔] Phase 9: Hardening — security audit (2 CRITICAL, 2 HIGH, 3 MED, 2 LOW — all fixed & re-tested), HQ + Admin dashboards wired to real data, DEPLOY.md, .env.example.

## Working Memory
### Active Context
- **DB live on MySQL:3307**, db `blue_travel`, root/no-pass. `php artisan migrate:fresh --seed` rebuilds clean demo.
- **Demo data is fully seeded** — `migrate:fresh --seed` rebuilds the whole showcase (ShowcaseSeeder = agent sale → provider approve → confirm → paid → MLM cascade → approved wallets; CustomerPortalSeeder = customer profile, referred customer, online booking + deposit, support ticket). Never hand-make demo records in tinker again.
- Staff back-office = single guard, RBAC role (super_admin/hq/admin) at `/hq/*` + `/admin/*` + shared `/manage/*`. Agent + Customer are public portals on the blue mockups; Provider uses admin layout w/ custom nav.
- Two Blade layouts: `layouts/admin` (BS5 staff) + `layouts/agent` (mobile phone shell for agent portal).
- Verification pattern: `php artisan tinker --execute="..."` for service flows + `php artisan serve --port=8901` + curl-login for HTTP/view checks. `grep -viE "warning|redis"` to strip noise.

### Decisions Made
- Commission = **MLM multi-level, dynamic depth** (admin CRUD commission_levels = cascade depth; L1 8/L2 4/L3 2). Base = booking total. Orphan→HQ. Pending→HQ approve→wallet (KPDN safeguard).
- **FPX vendor = Billplz** (decided 2026-07-20, more gateways later) — built behind a pluggable driver layer so adding ToyyibPay/senangPay/Bayarcash is one class + one config block.
- Finance + report charts = pure CSS bars (no Chart.js dependency).
- Reports = one `ReportService::build(key, filters)` returning a uniform payload (columns/rows/totals/kpis/chart) so the view, PDF and Excel/CSV exports all consume the same shape.

### Blockers / Open Questions
- FPX real vendor, SMS/WhatsApp vendor — still parked (Planning §10). Sandbox gateway swappable.
- KPDN Direct-Sales licence needed before real MLM payouts (compliance) — commission default-deny gate already in place.

## Demo Logins (all password: `password`) — `DatabaseSeeder`, dev only. Live boxes use `ProductionSeeder`.
- Staff: super@ / hq@ / admin@bluetravel.com · Provider: provider@bluetravel.com (linked to Provider #1)
- Agents: agent@bluetravel.com (root BT-AG001) · nadia.agent@ (BT-AG002) · imran.agent@ (BT-AG003)
- Customer: customer@bluetravel.com · Demo booking BK-2026-00001 (confirmed+paid showcase)

## Session Recap
> This section survives resets. Keep it under 30 lines.

### What Was Done
- Iris init → Planning.md (4 modules, 10 phases), MemoryCore #41, session memory.
- **Phases 0–8 built & verified** on MySQL:3307:
  - P0 Foundation · P1 Core Catalog · P2 Booking Engine · P3 Payments & Finance (FPX sandbox, receipts, refunds) · P4 Commission (dynamic-depth MLM) · P5 Agent Gamification · P6 Marketing & Support · P7 Reports & Analytics · P8 Customer Portal.
- Full per-phase detail (files, services, verification) lives in MemoryCore profile #41.

### Where We Left Off
- **[✔] UPCOMING TRIPS screen (2026-07-26).** From a 2nd client reference (hotel-app style). **Added as a NEW screen — My Customers untouched** (Fakrul chose this over replacing it). Route `agent.upcoming` at `/agent/upcoming` (NOT `/bookings/upcoming` — `{booking}` would capture it), new nav item (nav now 7 items, fits). Card = customer name · arrival–return · nights · pax · package title, plus status badge. **Arrival ⇄ Reservation toggle** switches sort AND the date-group heading; the list is always upcoming either way. Upcoming = arrival ≥ today, excluding cancelled/rejected/refunded/completed. **Arrival sorting is done in PHP** because the arrival date lives on two columns (`package_dates.depart_date` or `travel_date` for open-dated) — fine for one agent's list, revisit at scale. New `Booking` helpers: `arrivalDate()`, `returnDate()`, `nights()`, `paxSummary()` (nights fall back to `packages.duration_nights`; "1 adult, 1 child" not pluralised). **Open-dated bookings have no packageDate row** — the query needs the `doesntHave('packageDate')` branch or they disappear. 10 tests. Suite → **117 passing**.
- **[✔] SELECT2 on `/agent/bookings/create` (2026-07-26).** All selects (package, departure, customer, room type) are searchable. **Bundled via Vite, not CDN** — the agent portal is an installed PWA whose SW caches `/build/*` cache-first but never HTML. New `resources/js/agent.js` + `resources/scss/agent-select2.scss`, added to `vite.config.js` input and `@vite`'d from `layouts/agent` (**that shell previously had no JS bundle at all**). **jQuery pinned to 3.x** — `npm i jquery` pulls 4.0 which drops APIs Select2 4.0.13 needs. Opt-in per form via `data-select2` (create only; edit untouched); opt out per field with `data-no-select2`. `minimumResultsForSearch: 0` so search shows on short lists. Dropdown parented to the field's `.card` or it escapes the phone frame. **Select2 ate the "➕ New customer…" option** (it had `value=""`, and Select2 drops every empty-value option as the placeholder) — the agent could no longer register a customer from the booking form. Now posts sentinel `__new`, normalised to null in `store()` before validation. `AgentCreateBookingTest` (5 tests) pins it. Suite → **107 passing**. **Two traps:** (1) Select2's CJS build **does not self-register** — `module.exports` is a `(root, jQuery)` factory, so `import 'select2'` leaves `$.fn.select2` undefined; call `select2(window, $)`. (2) **Select2 fires jQuery's `trigger('change')`, which never reaches `addEventListener` listeners** — `fillPackage()`/`recalc()` are bound that way, so package selection would silently stop loading departures/rooms/pricing; fixed by re-dispatching a native `change` on `select2:select`. Dynamic room rows call `initSelect2(row)`.
- **[✔] OPEN EDIT WINDOW (2026-07-26) — SUPERSEDES Q1.** Agents may now edit **any** booking without an admin revision request; only `completed`/`cancelled`/`rejected`/`refunded` are locked (`Booking::AGENT_LOCKED_STATUSES`). Every resubmission lands on **Submitted** (`pending_verification`). Suite → **102 passing**. **Integrity work this forced:** (a) a confirmed booking's **seats are released** on resubmit, else re-confirming double-counts them; (b) `confirmed_at` nulled; (c) if the total moved and a live Commission exists → **reverse + recalculate** (same policy as approved amendments); (d) invoice/voucher regenerate on re-confirm. Reducing a paid booking below `paid_amount` is still blocked — that's the "forfeited deposit" case from the new Booking Amendment spec, not yet built. **⚠️ FLAGGED: two overlapping paths now exist for a confirmed booking** — direct edit (takes it out of confirmed) vs Request Amendment (keeps it confirmed, moves seats). Needs a decision before agents see both.
- **[✔] Phase 10 MOCKUP-CONFORMANCE PASS (2026-07-26) — all 6 client screens now match.** Suite 93 → **99 passing**, re-walked in Chrome. Added: **`booking_pax.age`** (migration `…100006`) so the diff reads **"Child Age: 4 years → 6 years"** exactly as drawn (dob kept for passports) · **screen 6 success page** (`bookings.submitted`) · **screen 5 split from 4** (`bookings.confirm`, review ends in "Submit Again ›") · **"Approved" status** for `waiting_provider_confirmation` + own tab · **Status Guide legend** (`Booking::AGENT_STATUS_GUIDE`) · **"My Customers"** list retitled with search (name/phone/booking-no), travel date, customer name as headline, **View + Edit** buttons · **− / + pax steppers** · pickup `<datalist>` · receipt **View / Replace File** buttons · Revision History moved to the **sidebar** (permanent, not in a tab) · **Customers** in bottom nav. **Two defects fixed:** (a) `.btn` had no `text-align`/`text-decoration`, so every `<a class="btn">` in the agent portal rendered left-aligned and underlined; (b) phantom diff row `Travel Date → —` because the form blanks travel_date on fixed-date packages — now normalised at staging, not only at apply. **Not matched on purpose:** mockup's "Tasks"/"Profile" nav items don't exist in this system; "Completed" kept as a 7th status label since hiding a real state would be a regression.
- **[✔] Phase 10 BROWSER-VERIFIED (2026-07-26) — 3 real bugs found that 91 passing tests could not catch.** Demo data reseeded on :3307 (`migrate:fresh --seed`); full loop driven in Chrome (admin flags → agent banner → edit → Save as Draft → Review diff → confirm → resubmit → admin reads v2) plus the amendment flow over HTTP. All screens rendered correctly. Suite 91 → **93 passing**. **Bugs:** (B1) **Save as Draft was blocked by HTML5 `required`** on the departure select — an agent on a fixed-date booking with no departure literally could not save a draft; fixed with `formnovalidate`, because a draft must never be gated on completeness. (B2) **`apply()` left `travel_date` null while `package_date_id` was set** — `create()` derives one from the other, `apply()` didn't; the agent screen hid it via a fallback but reports/exports read blank. (B3) **Moving a booking from *no departure* onto one never reserved seats** — the `increment` sat inside an `if ($oldDate && …)` guard = overbooking hole. Also fixed: `requested_date` was required even when a scheduled departure was picked. **Lesson: feature tests POST directly and never exercise client-side validation or form-cleared fields — a browser pass is not optional.** **Tab overflow FIXED (2026-07-26):** `.seg` is now a proper scroll strip — `flex:0 0 auto` on chips (they were squashing), hidden scrollbar, scroll-snap, trailing spacer, and a 6-line script in `layouts/agent` that scrolls the **active** chip into view so a deep-linked `?tab=` is never off-screen. No permanent edge-fade mask — it looks like a rendering artifact at the end of the scroll.
- **[✔] Phase 10F BUILT — amendments on confirmed bookings (2026-07-26). PHASE 10 NOW COMPLETE (A–F).** Migration `2026_07_26_100005_create_booking_amendments_table`; `BookingAmendment`; `BookingService::requestAmendment/approveAmendment/rejectAmendment/applyAmendment`; agent "Request Amendment" card + staff "Amendment Requests" panel with inline approve/reject. Suite 75 → **91 passing**. **Decisions applied:** Q11a **agent only** (customers can't amend); Q11b **reverse + recalculate** commission when the total moves; Q7 **no gamification effect — needed no code** (gamification fires only inside `verifyPayment()` behind `isFullyPaid()`, and revisions are blocked once commission exists, so resubmit-farming is structurally impossible). **Types:** `travel_date`, `pickup`, `other` — `other` is recorded only, staff action it by hand. **⚠️ KNOWN LIMITATION:** the reverse-and-recalculate branch is **wired but unreachable and untested**, because no shipped amendment type can move `total_amount`. Anyone adding a price-changing type (package/room/pax) must write a real test for that branch first. Seat transfer verified: availability check adds back the booking's own pax; a full departure is refused with nothing moved.
- **[✔] Phase 10E BUILT — admin tabs & revision history (2026-07-26).** `manage/bookings/show` restructured into **Details / Activity Log / Documents** nav-tabs; Timeline moved out of the sidebar into the Activity Log pane (renamed "Activity History") with a "view changes" deep-link per entry that has a `booking_version_id`; Revision History panel newest-first (version · reason · author · timestamp); `manage.bookings.versions.show` viewer + shared `_diff.blade.php`; `BookingTimeline::version()` relation. Suite 64 → **75 passing**. **Perf:** `versions` eager-load projects columns explicitly so the list never pulls `payload`/`changes`, `author:id,name` eager-loaded — proven by a test asserting a 2-version and 4-version booking render in **identical query counts**. **Test-design lesson:** my first N+1 test matched any `users … in (…)` query and so counted *batched* eager loads — it could never have detected a real N+1. Query-count-invariance is the honest check. **Also:** a DOMDocument/XPath test asserts the 3 panes are **siblings, not nested** (a stray `</div>` silently swallows one tab into another).
- **[✔] Phase 10D BUILT — diff, confirm, resubmit (2026-07-26).** `BookingRevisionService::price()/diff()/apply()/applyReceipt()/latestVersionPayload()`; `BookingService::resubmitRevision()` (one transaction, `lockForUpdate`, status re-asserted after the lock) + `assertResubmittable()`; `roomLines()` made **public** so a resubmit re-prices through the same path new bookings use. Routes review(POST)→review.show(GET)→resubmit = POST-redirect-GET. `review.blade.php` = grouped before/after + totals + confirm checkbox (validated **server-side** via `accepted`). Suite 49 → **64 passing**. **Three real bugs the tests caught:** (1) `assertResubmittable` didn't pass `rooms` to `assertDateSelection`, which also enforces "at least one passenger" — every resubmit failed, AND it made the seat test pass for the wrong reason; (2) **the diff was computed after `apply()`**, but `apply()` rebuilds `booking_pax` wholesale so every passenger got a new id and the frozen diff reported the whole manifest as removed+re-added — diff now runs BEFORE apply against the draft payload; (3) `Booking::timeline()` used bare `latest()` so same-second events ordered arbitrarily → now `latest()->latest('id')`, and `log()` returns the row so it can be linked to a version.
- **[✔] Phase 10C BUILT — revision store + agent edit + Save as Draft (2026-07-26).** Migration `2026_07_26_100004_create_booking_version_tables` (`booking_versions` immutable+numbered, `booking_drafts` mutable one-per-booking-per-user, `booking_timeline.booking_version_id`, `payments.superseded_by`); rollback+re-apply verified. `BookingRevisionService` = snapshot/stage/formPayload/draftFor/discardDraft/snapshotVersion/ensureInitialVersion. Agent routes edit/draft/draft.discard/draft-slip. **Rooms JS extracted to `_rooms-js.blade.php` and `form.blade.php` refactored onto it — not forked.** `edit.blade.php` follows the mockup section order with flagged fields tinted amber. **THE load-bearing test passes:** a draft save that rewrites customer/pickup/notes/deposit/rooms/pax leaves `bookings`, `booking_rooms`, `booking_pax`, `customers`, `payments` byte-identical — only `booking_drafts` gains a row. Suite 36 → **49 passing**. `payments.superseded_by` exists but is not written until Phase D's `apply()`.
- **[✔] Phase 10B BUILT — admin requests a revision (2026-07-26).** Migration `2026_07_26_100003_create_booking_revision_requests_table` (remark + `fields` JSON + open/resolved/cancelled). `BookingRevisionRequest` holds the **append-only `FIELDS` registry** — 15 keys, deliberately limited to what an agent can actually change so admin can't flag a field the agent is powerless to fix (Q5). `BookingService::requestRevision()` / `recallFromProvider()` / `assertRevisable()`; `manage.bookings.revision` route + `#revisionModal` (remark + grouped checkboxes); amber "Admin asked you to fix this" banner + field chips on the agent booking. **Guards:** blocked once a non-reversed Commission exists (else a re-priced resubmit silently keeps the old commission), blocked when provider approved **and** paid, and `confirm()` now throws while an open request exists. A booking out with the provider is **recalled** (provider_status → pending, `sent_to_provider_at` nulled) so they never act on stale data. **Agent-only notification** — Q8 (customer visibility) still open, so the customer is deliberately not told; a test pins notification count at 1. Suite 23 → **36 passing**. **Q1 ANSWERED: agents may edit in `draft` + `needs_revision` only.** **Deviation from plan:** Vault grouped all 3 tables into one migration; split so Phase B ships alone — `booking_versions` + `booking_drafts` (and the lazy `initial` snapshot) move to Phase C.
- **[✔] Phase 10A BUILT — vocabulary & schema foundation (2026-07-26).** Migration `2026_07_26_100002_add_revision_flow_to_bookings_table` (up→down→up verified on MySQL:3307): `needs_revision` **appended** to the status enum, `pickup_location` / `arrival_time` / `revision_no` / `revision_requested_at` / `resubmitted_at`, 3 composite indexes. `Booking::AGENT_STATUS` + `AGENT_TABS` collapse the 10 staff statuses into 6 agent labels; agent list now filters on `?tab=` (a tab = several statuses, so "Submitted" can't hide provider-side bookings). Pickup on both booking forms + both detail screens. `whereIn('status')` audit done — `needs_revision` deliberately **excluded** from every "pending" KPI (it waits on the agent) and given its own tile on HQ + agent dashboards + a Manage tab count. Tests 15 → **23 passing** (`tests/Feature/AgentBookingStatusTest.php`). **Two traps §13 hadn't predicted:** (1) tests run on **SQLite**, which enforces enums via `CHECK`, so the raw MySQL `ALTER … MODIFY` broke 8 tests — `setStatusEnum()` now branches on `DB::getDriverName()`; (2) `down()` couldn't drop `(agent_id,status,created_at)` because MySQL had folded `bookings_agent_id_foreign` into it and refused to drop the FK's last supporting index — `down()` re-creates the plain index first. **DB note:** the local :3307 `blue_travel` holds the **ProductionSeeder** state (1 super-admin, zero bookings), not demo data — do NOT `migrate:fresh --seed` without asking.
- **[✔] Phase 10 PLANNED — Agent Revision & Resubmission Loop (2026-07-26).** Client mockup "Edit Customer & Submit Again" analysed and written up as **Planning.md §13**. Planned by Sage (scope) + Atlas (architecture) + Vault (schema); §13.2 records the 10 arbitration calls so they aren't re-litigated. **Framing: the mockup says "Edit Customer" but ~90% of the fields are BOOKING data — built on `Agent\BookingController`, not `Agent\CustomerController`.** Six sub-phases A–F, tasks #2–#7, sequentially blocked. **Nothing built yet — 11 open questions in §13.8, three of them blocking.** Key traps recorded: (1) `needs_revision` must be APPENDED to the enum — MySQL stores ENUM by ordinal, mid-list insertion silently re-labels live rows; (2) `$table->enum()->change()` is a no-op on Laravel 12 (no doctrine/dbal) — use raw `DB::statement`; (3) `CommissionService::calculate()` refuses to re-run once a commission exists, so a re-priced resubmission on a paid booking keeps the OLD commission forever → block `requestRevision()` when a live commission exists; (4) Save as Draft writes ONLY to `booking_drafts` — the live booking/pax/rooms are untouched until Confirm, and status never flips to `draft` (it would drop out of the admin queue); (5) payload snapshots store display labels beside every FK so the diff renderer needs zero lookups.
- **[✔] Multi-room booking (2026-07-26).** Pricing tiers are now **room types** (`package_pricings.capacity` = pax/room; Quad/Triple/Double/Single seeded at rising per-pax rates). A booking holds **many** room lines via new `booking_rooms` (prices snapshotted, so re-saving a package never rewrites an issued invoice). `BookingService::roomLines()` normalises the posted `rooms[]`; **no rooms posted → one line on the chosen tier**, which keeps the customer portal + seeders working. Booking pax columns are the roll-up; legacy rate columns keep room #1's. **Commission**: `paxBreakdown()` now returns the fare-weighted average per pax type across rooms, so `count × price` reproduces the exact blended base (verified: 2 Quad @8500 + 1 Triple @9434 → base RM 26,434, not 3×either). Seat check sums all rooms. Rooms shown on manage/agent booking pages + itemised per room type on the invoice. Infants never force an extra room.
- **[✔] Agents register their own customers (2026-07-26).** Agents could only book customers already assigned to them, with no way to add one — the booking form just said "No customers assigned to you yet." Now: `Agent\CustomerController` (index/create/store/edit/update) at `/agent/customers` with **3 fields only — Name, Phone No., Email(optional)** — `agent_id` forced to the caller, edit/update 403s on someone else's customer. On the booking form the select gains a "➕ New customer…" option that reveals inline name/phone/email; **when the agent has no customers the select is dropped entirely and the inline fields are the form**. `required_without`/`required_with` rules mean either a picked customer or a filled-in one. Customers quick-action added to the agent dashboard.
- **[✔] Package date modes — fixed / open / both (2026-07-26).** New `packages.date_mode` enum: `fixed` (scheduled departures only, seats enforced), `open` (traveller names the date, no seat cap), `both` (either). Bookings already had `package_date_id` + `travel_date` nullable — nothing enforced them. Now `BookingService::assertDateSelection()` is the single rule, called by all 3 booking controllers before `create()`; it also checks seats (`seats_total = 0` means uncapped). `create()` overwrites `travel_date` from the chosen departure so the two can never disagree. `Package::bookableDates()` (open + seats left) replaces the ad-hoc filters. Package form has a mode selector that hides the departures card for `open`; an **active** `fixed` package with no departures is rejected on save (drafts allowed). Seeder demos all 3 modes.
- **[✔] Agent registration — both routes (2026-07-25).** Was a real gap: nothing could create an agent (AgentController had index/show/update only, StaffController offers hq/admin only, `/register` hardcodes customer, and the login's "Request an account" was a dead toast). Now: **HQ create** at `/manage/agents/create` (name/email/phone/password/tier/status + optional upline agent code → `AgentTreeService::register`) and **public self-signup** at `/agent/register?ref=CODE` landing `status=pending` (LoginController's status check already blocks sign-in) → HQ approves on the agent show page. `User::nextAgentCode()` auto-assigns BT-AG### from the highest existing suffix. Pending-count banner on the agents index; recruitment link + copy button on the agent Network page.
- **[✔] Launch prep (2026-07-25).** Guest-middleware redirect made role-aware (`redirectUsersTo` in `bootstrap/app.php`) — a signed-in user hitting any `/login` used to bounce to `/` because the framework's `RedirectIfAuthenticated` falls back to the route named `home`; now goes to their own portal. Demo credential hints + demo-account placeholders stripped from all 4 login pages. **`ProductionSeeder`** added: `php artisan migrate:fresh --seeder=ProductionSeeder --force` = super admin `superadmin@bstravel.agency` only (+ `tier_period_processed` baseline), nothing else. Verified: 20 staff/public URLs all 200 on an empty DB. VPS note: Vite 7 needs Node 20.19+/22.12+ — Ubuntu apt's Node 18 dies with `crypto.hash is not a function`; build with `npm run build`, never `npm run dev`.
- **[✔] Per-package agent commission + senior pax (2026-07-24).** Commission now configured per package: dynamic levels, fixed↔% toggle, per pax-type values (adult/child/senior/infant). New `package_commission_levels` table + `PackageCommissionLevel` model; `CommissionService::calculate` rewritten (packageRows percent=count×fare×%, fixed=count×flat; falls back to global `commission_levels` when a package has none → showcase PKG-0001 still 1360/680/340). Senior is now a first-class pax category (senior_price on pricing/booking, `senior` in booking_pax enum, senior counter on all 3 booking forms). Admin UI = dynamic "💰 Agent Commission" card on package edit form. Verified: Bali percent 1014.40/507.20, Tokyo fixed 950/380, 15 tests pass. Fixed: infant commission input was cut off (col-md-1 → col-md-2). Anti-pattern: `}}C@if(...)` glued directive not compiled — use inline ternary.

### (prev) Where We Left Off
- **[✔] Public landing page rebuilt & wired to live data (2026-07-20).** `Route::view('/','welcome')` → `CatalogController@home`; the old 49-line placeholder is now a full marketing page.
  - Controller feeds: featured packages (falls back to any active if <3 featured), category chips **with live counts**, unique destinations, next open departures (seats-left computed), stat counters (packages / destinations / customers+bookings / providers) and `Company::current()` for the footer.
  - Sections: sticky nav (transparent → frosted white on scroll) · gradient hero + search form that GETs to `catalog.index` (q + category) · portal-access glass card · stat strip · featured package grid · popular destinations + next departures · why-us · how-it-works · agent-recruitment CTA · footer with real company reg/licence/phone/email/address.
  - No photos in the DB, so each package card paints a **category-coloured gradient + emoji** and swaps to `cover_image` automatically once uploaded. Interactions are vanilla JS only (IntersectionObserver reveal, animated counters, sticky nav) — no new dependency.
  - Verified in Chrome: `/` 200, real packages/prices/seats/company data rendered, chips + search hit `/packages` 200.
  - **Fixed:** default `ExampleTest` (hits `/`) now uses `RefreshDatabase` — it 500'd once `/` started querying packages. **15 tests pass.**
- **README.md rewritten** (was the stock Laravel one) + project **pushed to GitHub** `wafazz/blue-star-travel` (main). `.gitignore` extended to exclude `blue-star.pdf` (client-confidential spec).
- **Agent + Customer portals are now PWAs (2026-07-20)** — installable from the login page, as asked.
  - `public/manifest-agent.webmanifest` (name "Blue Star Agent", scope `/agent/`, start `/agent/login`, shortcuts: New Booking / Wallet / Leaderboard) and `public/manifest-customer.webmanifest` (name "Blue Star Travel & Tours", scope `/`, start `/login`, shortcuts: Browse Packages / My Trips). Separate manifests = two distinct home-screen apps.
  - Icons generated with PHP GD from `logo-icon.png`, **flattened onto white** (iOS renders PNG alpha as black): `icon-192`, `icon-512`, `icon-maskable-512` (logo at 72% for the Android circular safe zone).
  - `resources/views/partials/pwa.blade.php` — manifest link + `theme-color` + iOS `apple-mobile-web-app-*` tags + SW registration + `beforeinstallprompt` capture. Included in `layouts/agent`, `layouts/customer`, `auth/agent-login`, `auth/customer-login`, `auth/customer-register`. "⬇️ Install … app" button (`data-install-app`, hidden until the prompt fires) on both login pages.
  - **`public/sw.js` — security-shaped caching.** These portals show passports, bookings and commission on shared phones, so **HTML responses are NEVER cached**: navigations are network-only with `/offline.html` as the failure fallback; only `/images/*`, `/build/*` and static file extensions are cache-first. `/pay/*`, `/download` and `/export` are skipped entirely. Verified in Chrome: cache contained only 5 static assets + offline.html, zero HTML pages.
  - Verified live: SW registers and activates (scope `/`), both manifests parse, `beforeinstallprompt` fires (Chrome considers them installable), install buttons appear, and with the **server killed** a navigation renders the branded offline page from cache. 18-URL regression + 15 tests still pass.
  - **iOS Add-to-Home-Screen hint** on both login pages (`data-ios-install`, ships `hidden`): iOS has no `beforeinstallprompt`, so it shows written instructions with an inline iOS share glyph — "Install …: tap ⎋ Share, then **Add to Home Screen**". Revealed only when iOS **and** not already installed (`navigator.standalone` / `display-mode: standalone`), so it never double-ups with the Android install button and disappears once installed. Detection covers iPhone, old iPad, and **iPadOS 13+ which reports as MacIntel** (disambiguated by `maxTouchPoints > 1`) — verified true for iPhone/iPad/iPadOS and false for Android, macOS and Windows.


- **Payment Gateway Configuration page added (2026-07-20)** — `/manage/payment-gateway`, `Manage\PaymentGatewayController` + `manage/settings/payment-gateway.blade.php`, nav under Company. **HQ/super_admin only** (`role:super_admin,hq` inside /manage) since these are money credentials — the `admin` role gets 403 and the nav link is hidden.
  - Settings live in the `settings` table (`payment.driver`, `payment.billplz.key|x_signature|collection_id|sandbox`) and **override .env**; `PaymentGatewayManager::configFor()` merges file/env defaults with the DB values, so no redeploy is needed to change gateway.
  - **Secrets encrypted at rest** (`Crypt::encryptString`, decrypt wrapped in try/catch so a rotated APP_KEY degrades to the env fallback instead of crashing). The form only ever shows `••••••••3456`; submitting blank keeps the stored value.
  - Guard: switching the active gateway to Billplz is refused unless key + X-Signature + collection ID are all present. **Test Connection** hits `GET /v3/collections/{id}` and reports 401 (bad key) / 404 (bad collection) / OK with the collection title + sandbox-vs-live.
  - Verified: page 200 for super_admin, 403 for admin, nav hidden for admin · half-configured switch refused · save → encrypted rows (grep for plaintext = none) · blank-secret keeps value · manager resolves BillplzGateway with the DB creds · **webhook then validated against the DB-stored X-Signature (correctly signed → accepted, forged → 403)** · sandbox checkout 404s while a live driver is active. Dev env reset to `sandbox` with the dummy credentials cleared afterwards. 15 tests still pass.


- **Billplz integrated (2026-07-20)** — Fakrul chose Billplz first, more vendors later, so it was built as a **driver layer**: `config/payments.php` (`PAYMENT_GATEWAY` selects the driver) + `App\Services\Gateways\PaymentGatewayDriver` contract + `BillplzGateway` / `SandboxGateway` + `PaymentGatewayManager`. Adding a vendor later = one class + one config block; the routes are already driver-generic (`/pay/webhook/{driver}`, `/pay/return/{driver}`).
  - Flow: `initiate` creates the pending payment → Billplz **create bill** (amount in cents, HTTP Basic with API key as username) → redirect to Billplz → signed webhook → **re-query the Billplz API** → settle. Credited only when the VENDOR confirms `paid` AND the amount covers it; short payments are logged for manual review, repeat callbacks idempotent.
  - Signature: HMAC-SHA256 over `key.value` pairs joined by `|`, where the **concatenated strings are sorted, not the bare keys** — that's what puts `paid_amount…` before `paid…` in Billplz's own example. Verified against their documented example in a test.
  - `pay/webhook/*` is CSRF-exempt (server-to-server, no browser) — the signature IS the authentication. Documented in `bootstrap/app.php`.
  - Tests: `tests/Unit/BillplzSignatureTest` (6) + `tests/Feature/BillplzWebhookTest` (7) — forged signature, unsigned payload, tampered amount, vendor-says-unpaid, short payment, replay, unknown ref. **15 tests pass** overall.
  - `.env` keys: `PAYMENT_GATEWAY`, `BILLPLZ_KEY`, `BILLPLZ_X_SIGNATURE`, `BILLPLZ_COLLECTION_ID`, `BILLPLZ_SANDBOX`. Still `sandbox` locally — **Fakrul must paste real sandbox credentials to run a live end-to-end test**. `DEPLOY.md` §7 rewritten with setup + how-a-payment-is-trusted + how to add the next gateway.


- **Official Blue Star logo applied system-wide (2026-07-20).** Source `LOGO-02.png` → `public/images/`: `logo.png` (1000px, UI), `logo-icon.png` (512px, star cropped — sidebar/app icon), `logo-print.jpg` (420px flattened, PDFs), `apple-touch-icon.png` (180), `public/favicon.png` (64). Built with `sips` (`-c H W --cropOffset y x` for the star crop).
  - Placed: all 4 login screens + register (white tile on the blue hero, replacing the 🌏/✈️ emoji + "Blue Travel" wordmark — the logo carries the name), admin sidebar (icon + "Blue Star"), landing nav, and all 4 PDF templates (invoice/receipt/report letterheads; voucher gets a white plate on its navy banner). Favicon partial `partials/favicon.blade.php` included in all 10 standalone `<head>`s.
  - `Company::logoPath()` returns a print-safe path for DomPDF, preferring the uploaded company logo.
- **Bug found & fixed while doing it:** Fakrul had uploaded the logo via Manage → Company Profile as a 3200×3200 alpha PNG. DomPDF decodes PNG alpha uncompressed (~41MB) and **every PDF in the app died with a memory-limit fatal**. `Company::logoPath()` now builds and caches a downscaled flattened JPEG beside the upload (GD, memory limit lifted only for that one-off conversion) and falls back to the bundled logo. Any large logo upload is now safe.
- Verified: 16-URL regression across all portals 200 · invoice/voucher/receipt/report PDFs regenerate and visually show the logo · report PDF export 200.


- Phase 9 done — **project is code-complete**. Ran a full security audit (all controllers/services/models/routes/views), verified each finding **by exploit** before fixing and re-tested after:
  - **C1 CRITICAL** booking PDFs were on the PUBLIC disk at a guessable path → fetched a real invoice with no session (passport numbers, prices). Now private disk + random filename suffix, served only through the ownership-checked controller; leaked public copies purged.
  - **C2 CRITICAL** sandbox FPX callback trusted `result=success` from the payer's own browser → a customer cleared RM23,400 for free and minted RM3,276 commission. Simulated success now leaves the payment **pending staff verification**; checkout/callback 404 when `APP_ENV=production`.
  - **H1** payment slips → private disk + gated `payments.slip` route. **H2** throttle:5,1 on all logins + register (verified 429 on 5th).
  - **M1** provider `null === null` ownership bug; **I also found the list had the same bug** (`where('provider_id', null)` → `IS NULL`). Fixed both. Discovered the demo provider account was never linked to a Provider row — the portal only appeared to work *because* of the bug. Seeder now links it to Saudi Umrah Operator (2 bookings).
  - **M2** marketing upload mime allow-list · **L1** notification redirect restricted to APP_URL · **L2** redemption note validated.
- HQ Executive + Admin dashboards (the last two mockup screens) now fully data-driven; `Package::bookings()` added.
- `DEPLOY.md` written (requirements, first deploy, env, queue, security checklist, demo-data warning, **real-gateway integration steps**, KPDN note, smoke test, update procedure). `.env.example` made project-accurate.
- Verified: 39-URL regression across all 5 portals all 200 · cross-portal + cross-account isolation all 403 · public doc path 403 · legit staff payment-verify flow intact.
- **Remaining (client-side, not code):** pick the real FPX vendor and wire it per DEPLOY.md §7 · KPDN Direct Sales licence before real payouts · rotate APP_KEY/DB creds/demo passwords at deploy.
- Known deferred: booking modify/reschedule · manual document upload UI (flight ticket/visa/insurance) · credit notes · commission maturity-hold · leaderboard daily/weekly/yearly filters · real email/SMS/WhatsApp vendor · campaign builder · scheduled/emailed reports · customer loyalty-point auto-award.
